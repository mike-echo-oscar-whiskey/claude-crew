# Designs

This directory holds the approved technical designs the architect writes per story, one file per story named by the story's slug, and the decisions those designs record. The crew profile's `designs:` line points here so that `/crew:plan` and the architect binding resolve to a real path.
