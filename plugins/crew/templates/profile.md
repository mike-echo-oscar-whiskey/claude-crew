# Crew profile — <project name>

<!-- Read by every crew persona before acting. Keep it factual and short; link to
     CLAUDE.md and docs instead of duplicating them. Generated by /crew:init, then
     hand-edited. Commit it. -->

mode: optional            # optional | always  (always = every session starts in crew mode)
tracker: github <owner>/<repo>     # or: azure-devops <org> <project>  (not implemented yet)
default-branch: main
branch-pattern: task/{number}-{slug}
designs: docs/design/     # where /crew:plan writes technical designs (linked from the story)

## Stack bindings (one line per role; "disabled" turns a role off for this project)

product-owner: <where the backlog and product decisions live>
architect: <architecture docs, ADR location, layering rules>
frontend-engineer: <framework + version, app folders, shared UI, test runner>
backend-engineer: <language/runtime, projects, framework, test runner>
integration-engineer: <contract format, client generation, external systems>
event-sourcing-engineer: disabled
genai-engineer: disabled
agentic-ai-engineer: disabled
multitenancy-engineer: disabled
commercial-analyst: disabled
qa-engineer: <gate command, test conventions, known flake sources>
security-engineer: <identity provider, secret locations, threat model docs>
cloud-engineer: <cloud + region, IaC location, cluster, deploy script>
ux-designer: disabled
privacy-and-compliance: disabled
technical-writer: <README sections and docs that must track reality>

## Commands (in the order they must run)

build:   <command>
clients: <command or "-">
test:    <command>
gates:   <command>            # the one command that decides green/red
deploy:  <command or "-">

## Definition of done (walked explicitly by the delivery lead before a PR)

- gates green, exit code quoted
- <project-specific item>

## Exclusive lanes (single-occupancy resources; ask before use when another session may hold it)

- <resource>

## Triage (minimum crew per request kind)

| Request | Minimum crew |
|---|---|
| Question about the code | one specialist, read-only |
| Small fix, no contract change | specialist + qa-engineer |
| Fix touching auth, tenants, money or contracts | specialist + qa-engineer + security-engineer or integration-engineer |
| Backlog item | full pipeline: refine, plan, work, review |

## Gotchas every role must know

- <the incidents this repo keeps repeating; one line each, link to CLAUDE.md>
